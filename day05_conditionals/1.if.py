"""
if được sử dụng khi bạn cần kiểm tra điểu kiện nào đó trước khi thực hiện 
một hoặc nhiều câu lệnh. Các câu lệnh bên trong ì được thụt lề so với if.

"""
# and or not 
# kiểm tra xem n có nằm trong đoạn [50,100]
n = 60 
if n > 50 and n < 100 : print ("PyDaiLy!!")


#kiểm tra n có phải số chẳn
n = 20
if n % 2 == 0 :
    print("n là số chẵn")

#kiểm tra n có chia hết cho 7 không ?
n=21
if n % 7 == 0 :
    print(n,"chia hết cho 7 ")


#kiểm tra 1 số nằm trong đoạn[a,b]
a, b = 50, 100
m = 70 
if (m > a) and (m <= b):
    print("y")

#kiểm tra 
